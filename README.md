# 赵丽蓉个人主页

基于 React、TypeScript、Vite 和 Framer Motion 的个人作品集，可作为静态站点部署到 GitHub Pages。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## GitHub Pages

仓库 Settings → Pages 中选择 `GitHub Actions`，再将 `.github/workflows/deploy.yml` 推送到默认分支即可自动发布。
